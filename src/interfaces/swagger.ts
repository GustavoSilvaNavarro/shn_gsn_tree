export type SwaggerSpec = {
  title: string;
  openapiVersion: string;
  description: string;
  prefix: string;
  publicApiVersion: string;
  version: string;
};

export type ServerTypes = {
  url: string;
  description: string;
};
